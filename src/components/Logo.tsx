import logo from "@/assets/logo-dias-pires.png";
const Logo = ({
  className = ""
}: {
  className?: string;
}) => {
  return <div className={`${className}`}>
      
    </div>;
};
export default Logo;